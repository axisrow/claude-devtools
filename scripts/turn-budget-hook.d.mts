/**
 * Types for the standalone turn-budget hook — the calibration CLI
 * (src/cli/turnSpendStats.ts) imports the shared turn-boundary predicate
 * from the .mjs so both accountings use one definition.
 */
export declare function isTurnBoundary(m: unknown): boolean;
